{
  "name": "hello-world-app",
  "version": "1.0.0",
  "description": "A simple Hello World app with React frontend and Node.js backend",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"bun run --cwd backend dev\" \"bun run --cwd frontend dev\"",
    "start": "bun run --cwd backend start",
    "build": "bun run --cwd frontend build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
